= nav_bar inverse: true, brand: 'OnlineStore', responsive: true do
  = menu_group do
    =menu_item 'Categories', categories_path
  = menu_group pull: :right do
    - if user_signed_in?
      = menu_item t('.log_out'), destroy_user_session_path, method: :delete